services:
  - type: worker
    name: my-ai-secretary
    env: python
    pythonVersion: "3.11"
    buildCommand: "pip install -r requirements.txt" # rcloneのインストールを一時的に削除
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      # disksセクションを完全に削除